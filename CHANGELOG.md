# Change Log

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与语义化版本。

## [0.3.6] - 2026-09-15

### 修复 · 写寄存器块"找不到驱动源"（`always` 体被截断到第一个分号）

真实 RTL 里极常见的一种写法：

```verilog
always @(posedge clk)
  if (!rst_n)
    begin
      o_num <= 8'd0;        // ← 旧逻辑把块体就断在这里
    end
  else if (wr_en)
    case (addr)             // case 直接当 if-else 的语句体，没有 begin/end 包裹
      7'h28: o_num <= wr_data;
    endcase
```

解析器算 `always` 块体范围时只按"找到第一个 `;`"收尾，于是块体被判定为到复位分支的第一条赋值为止 ——
整个写寄存器块（几百行、几十个寄存器）都没被收集，表现就是这些寄存器**全部报"未找到驱动源"**，
而实际驱动它们的正是块内那句 `x <= wr_data`。

现在块体按**整条语句**收尾，覆盖：

- `if (…) stmt [else stmt]`（含 `else if` 链）
- `case / casex / casez … endcase`（可嵌套）
- `begin … end` / `fork … join*`
- `for` / `while` / `repeat` / `forever`（循环头 + 一条语句）
- 延时 / 事件控制（`#5`、`@(posedge clk)`）

修复后同一个信号能一路追出来：`过程块 → 块内参与运算的信号（写数据总线）→ 父层端口`。

### 修复 · "声明即赋值"没进数据流

```verilog
wire [7:0] s_cfg_num = hist_active ? r_hist_num : i_num;   // 声明处就带表达式
output reg [24:0] o_cnt = 0;                                // 声明处就带初值
```

解析器原来**跳过**声明里的初值表达式，于是这类信号：

- 上游："找不到驱动源"（其实驱动它的就是这句声明）
- 下游：别处引用它时也不会被算作"被谁读取"

现在把它记为一条赋值（`AssignStmt.inline`），两个方向都能追；界面上照实写"声明处赋值"，
不写成 `assign`（原文里没有这个关键字）。

### 修复 · 常量 / 初值驱动被误报"未找到驱动源"

右值里没有"本工程内可识别的信号"（常量、参数、宏、函数调用）时，旧逻辑直接跳过这条驱动 →
`wire x = 1'b0;` 这类信号被报成"未找到驱动源"。现在改为一条 `常量` 节点，
照实说明"右侧没有可识别的信号"。

### 改进 · 过程块节点的落点更准

- **上游**：`x <= 0;`（复位分支）与 `x <= wr_data;`（数据通路）都写同一个信号时，点击优先落到
  **有 RHS 来源**的那条 —— 节点行号与它带出的子节点在同一行，而不是把人带到复位赋值上。
- **下游**：`x <= net;` 这类"过程块内参与赋值"的节点，描述行也改到**那条语句行**（原来写 always 行，
  与跳转偏移指向的行不一致）。

### 工具

- 自检 **137 项**（新增 17 项）：新增 fixture 复现"if + else-if + case 直接当语句体""`always @(*) case … endcase`"
  "声明即赋值""常量驱动""块体就是一条赋值（无 if / 无 begin）"五种写法，断言块体范围正确（三个 always
  不互相吞并）、`lhsNets` 覆盖 case 内所有被写信号、能带出 RHS 源信号、上下游落点精度、inline 标记、
  常量不被误报。
- 缓存版本 `CACHE_VERSION` 3 → 4（解析结果结构变了，避免读到旧结构）。
- 真实工程目视核对：原先报"未找到驱动源"的多个寄存器现在都指到各自的赋值行，并继续向上追到父层写数据总线；
  声明即赋值的信号在上/下游都出现；抽查另一个工程的同一处代码，行号逐字未变（无回归）。

## [0.3.5] - 2026-09-15

### 修复 · "顶层输入端口 / 常量"这类节点点了不跳转

树视图与脑图都以节点自带的 `file` + `line` 作为"能不能跳转"的判据，而 `terminal`
（顶层输入端口 / 顶层输出端口 / 未找到驱动源 / 无下游负载）、`constant`（父层接成常量）、
`unconnected`（悬空 / 未连接）这几类节点**在 core 里创建时就没带位置** —— 于是点击没反应。
（框图视图不受影响：它用的是模块级数据，本来就带位置。）

现在每一类都补上**点了有用的落点**，并写进悬停提示：

| 节点 | 落点 |
|---|---|
| 顶层输入 / 输出端口 | 该端口在模块里的声明行 |
| 父层接成常量 | 父层那一次连接（文件名:行） |
| 悬空 / 未连接 | 同上 |
| 未找到驱动源 / 无下游负载 | 该信号在本模块的声明行（没有声明则退回模块头） |

### 工具

- 自检新增 3 项（共 120 项）：遍历多棵链路树，断言"**所有可跳转节点都必须带 `file` + `line`**"
  （仅分组 / 说明 / 可展开节点除外），并单独校验"顶层输入端口 → 端口声明行"、
  "常量 → 父层那一次连接"。这条断言是通用的 —— 以后新增任何节点类型，漏给位置都会被自检拦下。

## [0.3.4] - 2026-09-15

### 修复 · 一轮真实 AI 使用反馈里定位到的四处（含两个底层解析 bug）

- **内部信号的声明行号丢失**（`core/parser.ts`）：`wire / reg / ...` 声明登记进符号表时只记了字符偏移、
  没记行号，于是 Hover / 跳转 / AI 归纳位置全部退化成"模块头那一行"或空。
  实测里 5 个信号有 4 个的声明行是错的（报 `:2`/`:4`，真实在 `:193`/`:275` 这种位置），
  已逐个核对修正；顺带把 trace 根节点的"声明位置"也改成真实声明行（原先退化为模块头）。
- **函数 / 任务之后的模块体被整段吞掉**（`core/parser.ts`）：跳过 `function` / `task` 块时，
  把**起点关键字自己**也算进了嵌套深度，于是"多吞一层"直到文件末尾 ——
  该模块里它后面所有的例化、assign 全部消失，看起来就像"这个模块是空的"。
  真实案例：某 testbench 里第一个 `task` 之后 460 行的 DUT 例化全部不见（修好后能正确识别出那两条例化，
  顶层模块的"被例化"也终于看得见了）。结束关键字缺失时改为停在模块边界，不再牵连整个文件。
- **加密 / 二进制文件不再被静默当成空模块**（`core/indexer.ts`）：内容不是文本的 `.v`
  （有些工程把仿真模型直接放成二进制）以前会被当 Verilog 解析，从乱码里"碰巧"认出 `module` 字样，
  产出一个「端口 0 · 子例化 0」的假模块 —— 看着像代码有问题，其实是文件不可读。
  现在识别并跳过，`index` 报告里会列出跳过的文件与原因；`module` 查不到名字时也会提示这一可能。
- **trace 文本的两处渲染问题**（`tools/ai.mjs`）：① 被过滤掉的中间节点仍会被打印出来，
  既让层级看起来错乱、又让 `filter=cross` 的"跳过模块内中间变量"失效；现在只提升它的子节点、不打印自身。
  ② `assign X X` 这种同名重复（edgeText 与 label 撞名）收敛成 `assign X = …`。

### 工具

- 自检新增 1 项（共 117 项）：模块里带 `function` / `task` 时，其后的例化必须仍被识别。

## [0.3.3] - 2026-09-15

### 清理 · 去掉文档里残留的真实工程信息

- README / CHANGELOG / 源码注释里的示例，之前混进了真实工程的**文件名与模块名**（层次路径示例、
  框图 ASCII 图、搜索与命令行示例里都有）。现已全部换成中性示例（`top.u_a.u_b.s_data`、
  `data_path`、`mem_ctrl`、`my_rtl_project` 之类），仓库内容里不再出现任何具体工程的名字。
- `dist/` 下的历史版本 vsix 一并移除 —— 它们内部打包了当时的 README / CHANGELOG，
  等于把同样的信息又带了一份；新版 vsix 只包含清理后的文档。
- 已发布 Release 的正文与附件按同一口径清理。

## [0.3.2] - 2026-09-15

### 修复 · 三个会直接误导 AI（和人）的问题

- **`search` 的匹配语义**：原来只做"全名匹配"，`rd_time` 搜不到 `o_rd_time`，直接给出
  "命中 0" 的死胡同 —— 实测反馈里那句"搜不到"其实是这个原因，**不是 root 的问题**。
  现在：**默认包含匹配**；查询里带 `*` / `?` 时按通配式整体匹配；大小写不一致时自动再试一次
  （不区分大小写）并在标题里注明用的是哪种匹配。
- **网络等价类把互不相干的网络粘在一起**（`core/nets.ts`）：等价边是"父层网络 ≡ 子模块端口"，
  但同一个子端口被**多次例化接到不同网络**时（典型：被共用的 RAM / FIFO 叶子，如
  `xpm_dpram_simple.dina`），穿过它做传递合并会把整条数据通路上的名字粘成一个类 ——
  实测某 96 位总线因此拿到 **64 个"别名"**，其中大半是别的通道的总线，顺着查会跑到别的电路去。
  现在这类"汇聚端口"的等价边**不并**（遵循本文件既有原则：宁可漏也不并错）；这些连接改由
  `describe` 的驱动源 / 负载体现 —— 那里本来就带实例名，说得比"别名"更清楚。
- **路径口径的回归**：0.3.1 引入的 `[工程外]` 标注在别名 / 搜索结果里被应用了两次
  （对已经是相对路径的值又算了一遍），导致**工程内的文件被误标成 `[工程外]`**。
  现在路径只在源头相对化一次，JSON 与文本口径一致（相对路径 + 1 起行号）。

## [0.3.1] - 2026-09-15

### 改进 · 按一次真实 AI 使用反馈修的六处

背景：拿 MCP 接口在一个真实 DSO 工程上试用了 10 次调用，反馈里有两类问题 ——
**服务被绑死在一个工程上**、以及若干"AI 会误读"的输出。

- **服务不再绑定单个工程**：原来 `--root` 写死一个工程，换工程就得改配置重启。现在默认根 =
  **客户端声明的工作区**（MCP `roots`，即 IDE 打开的项目）；问不到才退回 `--root` / 当前目录；
  **8 个工具都多了一个可选 `root` 参数**，任何一次调用都能切到别的工程；各工程各自建索引
  （LRU 保留最近 4 个），切换不重启、不反复重建。`sigroute_index` 会报出已索引的工程根清单。
- **路径口径统一**：工程外的路径以前会以**裸绝对路径**混在相对路径里（实测 AI 直接照抄去打开、
  结果路径不存在）。现在一律标成 `[工程外] <绝对路径>`，工程内仍是相对路径。
- **`context` 的提示不再误导**：区分"文件在工程根内、但这一行不属于任何模块"与
  "该文件不在已索引的工程根内（只读了文本、没有语义信息）"。
- **`trace` 输出加上限**：默认 200 行，超出即截断并明确给出收窄办法（`filter=cross|renamed`、
  更小的 `depth`、或 `limit=<全量>`）—— 一次刷上百行既费 token 又抓不住重点。
- **`index` 的顶层候选收敛**：原来把 97 个候选一口气列出来（IP 包装、testbench 全在内）。
  现在按子例化数排序、标出"其中 N 个有子例化"、并以 `名字(例化数)` 形式只列前 12 个。
- **`context` 的"附近声明"收敛**：从 51 条收到 12 条，并给出总数。

### 工具

- 自检新增 7 项（共 113 项）：真跟一个"MCP 客户端"握手 —— 断言服务端主动反问工作区根、
  不带 `root` 时确实用了客户端声明的工程、`root=` 能切到另一个工程且两边结果不串、
  8 个工具都带可选 `root` 参数。

## [0.3.0] - 2026-09-15

### 新增 · 给 AI agent 的接口（MCP + 命令行）

AI 读 RTL 最费 token 的从来不是"看懂一行代码"，而是那几个跨文件的问题：
**这根信号从哪来、到哪去、穿过了谁、在哪儿改了名**。这里把插件同一套解析/追踪引擎开放出来：

- `tools/mcp.mjs`：**零依赖的 MCP stdio 服务**（手写 JSON-RPC，不引 `@modelcontextprotocol/sdk`），
  把 8 个工具挂给 CodeBuddy / Claude Code / Cursor 等：`index` / `search` / `describe` /
  `trace` / `aliases` / `hierarchy` / `module` / `context`。
  长驻进程会按文件指纹**增量刷新**索引 —— 改完代码再问，拿到的是新结果。
  `node tools/mcp.mjs --print-config --root <工程>` 直接打印客户端配置片段。
- `tools/ai.mjs`：同一份实现 + 命令行前端（`node tools/ai.mjs trace <信号> --root <工程>`），
  任何能跑 shell 的 agent 都能用；`--json` 给结构化结果。
- 两者共用 `src/core`，所以路径、改名点、等价类的结论与插件里显示的完全一致，
  不存在"两套解析结果对不上"的问题。
- 统一约定：**行号 1 起、路径相对工程根**（可直接喂给 `read_file` / `grep`）；
  `trace` 支持 `filter=cross|renamed`（只看跨模块 / 只看改名，省 token）与 `graph`（模块级拓扑）。

### 修复 · 内部信号的声明位置丢了

- `describeSignal` 对模块内部信号把声明行硬写成 `undefined`（core 里明明有 `sig.line`）。
  受影响的不只是 AI 层：**插件里对内部信号 Ctrl+点击拿不到定义、悬停里也点不到声明位置、
  声明源码那一块贴不出来**。现在补上，`decl` 小节连同上方注释一起显示。

### 工具

- **vsix 现在自带 AI 接口**（`tools/ai.mjs`、`tools/mcp.mjs`）：只装了插件、没 clone 仓库的人也能
  直接挂 MCP —— 它跑的是 `out/` 编译产物（与插件同一份代码），不需要 `npm install`，也不需要
  旧版那样的源码类型剥离。仓库里跑则优先 `out/`，`src/` 比产物新时自动回退源码
  （`SIGROUTE_AI_RUNTIME=src|out` 可强制）。
- 自检新增 18 项（共 106 项）：AI 层 8 个操作逐个断言（行号 1 起、别名反查、增量刷新、
  `context` 在声明行与例化行分别落在信号/例化上），并把 MCP 服务真的拉起来走一遍
  `initialize` → `tools/list` → `tools/call`，断言"只回应请求、不回应通知"、工具清单为 8。

## [0.2.9] - 2026-09-14

### 改进 · 定义跳转（Ctrl+点击）

- **落点精确到标识符**：原先返回的是「行首、零宽」的位置 —— 跳过去光标停在行首，
  既没有选中也没有闪烁高亮。现在返回 `LocationLink`，用 `targetSelectionRange`
  精确覆盖模块名 / 端口名（VSCode 拿它同时做折叠后的选区与闪烁高亮范围）。
  例化处的模块名与 `.端口名` 都走这条路径，跳过去就是灰底选中 + 短暂闪烁。
- **新增命令 `SigRoute: Ctrl+点击直接跳转（多处定义不再弹预览）`**：
  工程里通常还装着别的 Verilog 扩展（如 `mshr-h.veriloghdl`），它们同样注册了
  DefinitionProvider；VSCode 把多家结果合并后按"多处定义"处理，默认策略是弹候选预览窗。
  （**只有一个结果时它本来就直接跳** —— peek 分支要求 `references.length > 1`，
  所以这不是插件多返回了候选。）该命令把 VSCode 的
  `editor.gotoLocation.multipleDefinitions` 设为 `goto`，Ctrl+点击 一步到位；
  写入前有一次确认，写完的通知里可一键恢复。

### 工具

- 新增 5 项自检断言锁住跳转落点列号，覆盖两种最容易错位的情况：
  名字先出现在注释里、`i_a` 不该命中进 `i_a_wide`。

## [0.2.8] - 2026-09-14

### 修复

- **"`always @(*)` 的条件逻辑"节点同样落到块内该信号上**（上一版只修了"赋值给 xxx"那一类）。
  条件 / 索引里的读取（`if (s_ena)`、`mem[i]`）不会进入语句级数据流，原先只有名字没有位置，
  跳转只能选中 `always` 关键字。
  解析层现在记录块内每个标识符的**首次出现位置**（`AlwaysBlock.readRefs`），
  这类节点因此也能灰底选中块内那个信号，描述补上 `· 条件/索引中的 xxx`，悬停给出读取行号。
- 索引缓存版本号 +1（`AlwaysBlock` 结构变了，旧缓存自动失效重建）。

## [0.2.7] - 2026-09-14

### 修复

- **点"`always @(*)` 过程块"节点时，落到块内真正给该信号赋值的那一行并选中它。**
  之前只跳到 `always` 关键字那一行（等于没定位）。现在节点带的是赋值语句里**目标信号的精确偏移**，
  跳过去就是灰底选中 + 短暂闪烁；节点描述补上 `· 赋值给 xxx`，悬停提示给出语句行号。
  下游方向（过程块里读取该信号、再赋值给别的信号）原本就定位在目标信号上，未改动。

## [0.2.6] - 2026-09-14

### 新增

- 追踪信号（上游 + 下游）**增加 `Alt+Q` 作为备用快捷键**，与 `Ctrl+T` 并存，
  同样只在 Verilog / SystemVerilog 编辑器内生效。两个键随便按哪个都行，
  也可以在「键盘快捷方式」里删掉不需要的那个。

## [0.2.5] - 2026-09-14

### 新增 · 悬停里原样显示声明源码

- `decl` 小节会把**声明所在的那一行源码原样贴出来**（连同它上方紧邻的最多 3 行注释），
  以带语言标记的代码块渲染 —— 悬停里直接看到语法高亮和注释，不必再跳过去确认。
- 新增 `sigroute.hover.declSource`（默认开）可关掉，只保留位置链接。
- 源码块不计入 `hover.maxLines` 的"放不下就整节不显示"判断：它是信息框的主体信息。

### 变更 · 快捷键

- 追踪信号（上游 + 下游）的快捷键由 `Ctrl+Alt+T` 改为 **`Ctrl+T`**（macOS 为 `Cmd+T`）。
  仍然限定在 Verilog / SystemVerilog 编辑器内生效，所以不会影响其它文件里
  VS Code 默认的 `Ctrl+T`（转到工作区中的符号）。

## [0.2.4] - 2026-09-14

### 新增 · 悬停速查可配置

编辑器里悬停的信息框现在完全由设置决定：

| 设置 | 默认 | 作用 |
|---|---|---|
| `sigroute.hover.enabled` | `true` | 关掉后悬停不再弹任何信息 |
| `sigroute.hover.sections` | 全部六个 | 显示哪些小节、以及顺序：`decl`（声明与位置）/ `drivers`（驱动源）/ `loads`（负载）/ `parent`（上级模块连接）/ `path`（层次路径）/ `aliases`（同网络别名）。清空则只留信号名 |
| `sigroute.hover.maxRefs` | `12` | 每一节最多列几条（超出提示可调大） |
| `sigroute.hover.maxLines` | `40` | 整个信息框的行数上限，超出按节截断并提示 |

- 行数按"节"扣减：某一节放不下就整节不显示，不会出现半截图式的残缺内容。
- 悬停渲染逻辑抽到 `core/hover.ts`（纯字符串、可单测），并新增 9 项自检断言。
- 改这些显示类设置**不再提示重建索引**（只有 `includeGlobs` / `defines` / `inlineIncludes`
  等影响解析的配置才会提示）。

## [0.2.3] - 2026-09-14

### 修复

- **起点节点不再跳去声明处**：树 / 脑图里点根节点，现在回到"**你当时发起追踪的地方**"——
  光标所在处、悬停链接处、框图上点的那条连线处、端口列表里选的那个端口处。
  声明位置仍然保留，写在根节点的悬停提示里（`声明位置：xxx.v:NNN`）。
- **脑图的悬停信息框不再挡住信号**：改为贴着光标**右下方**弹出，并主动避开当前悬停的那一行；
  右边/下方空间不足时先贴边、再翻到光标上方。宽度改为跟随视图宽度
  （`min(320px, 100vw - 12px)`），窄侧栏里不会再被"挤"到左边盖住信号行。
- 框图的信息框同样加了"下方放不下就翻到光标上方"的处理，宽度也跟随面板宽度。

## [0.2.2] - 2026-09-14

### 改进

- **脑图默认只展开"直接关联两层"**（与树视图同一策略）：起点 / 上下游分组 / 直接关联的那一层展开，
  再下一层先收起，需要时手动点开 —— 不会再一屏几十行把真正关心的几条淹掉。
  只有一个子节点的链路自动展开，避免一路都是没有信息量的点击。
  工具栏改成「默认 / 展开 / 折叠 / 导出」：`默认` 回到两层、`展开` 全开、`折叠` 只留起点与分组。
  同一根信号再次渲染（点"继续展开"、重新追踪）时会保留你手动折叠出来的形状 ——
  折叠状态按"子节点下标路径"记忆，不受节点重建影响。
- **框图补上真实例化信息（包含 / 同层次 / 跨层次）**：
  - 方块第二行显示**真实例化名**（`实例 u_b`），悬停显示"在哪个模块中被例化"，
    以及同一模块在别处的其它例化名；
  - 新增**层次框**：同一个父模块下、同一列且相邻的兄弟实例用虚线框圈起来，
    标签写明"xxx 内的同层实例：u_a, u_b" —— 包含关系与同层次直接在图上看得出来，
    跨框的连线就是跨层次；工具栏 `层次框` 可关；
  - 连线悬停显示"经由实例"。
- **箭头不再被线宽撑大**：marker 改为 `markerUnits="userSpaceOnUse"`（固定 6 用户单位），
  重边/改名边不会再把箭头放大到挡住方块；连线终点留出间隙，箭头不再压住方块边框与模块名。

## [0.2.1] - 2026-09-14

### 修复

- **脑图在侧栏里根本读不了**：原实现是"中间一个根、左右发散的辐射式"，需要横向空间；
  资源管理器侧栏只有两三百像素，实际只能看到几个挤在一起的方块。
  改为**竖向缩进**布局：根在最上、逐层向下、缩进随深度自适应、文字按可用宽度截断、纵向滚动。
  上游绿 / 下游紫 / 改名橙加粗 / 黑盒黄虚线，折叠时显示 `(+N)`，点行跳源码、点开关折叠。
  辐射式与缩放平移一并移除 —— "看大图"由框图面板承担。
- **过滤状态跨视图互相影响**：在脑图里点了「只看改名」，切回树视图发现内容少了一大截，
  看起来像功能坏了。现在脑图**不再有过滤**，也不与树视图共享过滤状态；
  过滤只属于树视图（入口在它自己的标题栏，默认「全部」）。

## [0.2.0] - 2026-09-14

围绕"更好用"做的一轮增强：补齐日常交互闭环、把跨模块信息打通、削掉大工程下的固定开销。

### 新增 · 层次路径

- `SigRoute: 复制信号层次路径`（`Ctrl+Alt+C`）：一次给出 `top.u_a.u_b.s_data`，
  同一模块被多处例化时列出全部候选。Hover 里也直接显示，可点击复制。
- 悬停 / 引用 / 面板里的跳转不再需要一层层手动往上爬。

### 新增 · 网络等价类（改名反查）

- 索引阶段把"父层网络 ≡ 子模块端口"并成等价类，得到**同一根物理网络的全部名字**。
- `SigRoute: 查找同网络别名`：列出这根线在别的模块里叫什么、方向、位置，点选即跳转。
- Hover 新增「同网络别名」「层次路径」两段；查找引用（Shift+F12）现在会覆盖等价类里的
  其它模块（以前只看当前模块，信号一改名就只能查到一半）。
- 树节点/脑图节点提示里会带出别名清单。

### 新增 · 脑图视图（可与目录树一键切换）

- 资源管理器里新增 `SigRoute 信号脑图`：根在最上、逐层向下缩进（竖向布局）。
  侧栏只有两三百像素宽，辐射式布局放进来看不到东西，所以宽度让给文字、长度交给纵向滚动。
- 上游绿、下游紫、改名橙加粗、黑盒黄虚线；折叠时显示 `(+N)`，点行跳源码。
- `展开 / 折叠`（收起到起点 + 上下游两层）、`导出 SVG`。
- 视图标题栏按钮在 树视图 ⇄ 脑图 之间切换，记住选择。
- 脑图与树视图**互不影响**：过滤只属于树视图，在一边操作不会让另一边变样。

### 新增 · 树视图过滤与继续展开

- 过滤三态（标题栏按钮循环）：`全部` → `只看跨模块` → `只看改名`。
  `只看跨模块` 会跳过模块内部的中间变量，把符合条件的节点提升上来
  （可见层级与真实层级不同，getParent 已按可见关系维护）。
- 分支超限时不再只是一句"另有 N 个未展开"，而是**可点击**的继续展开：每点一次放宽 4 倍。
- 根节点下新增「链路摘要」：模块数 / 改名数 / 黑盒数 / 不确定数 / 最大跨模块层数。

### 新增 · 编辑器内

- CodeLens：模块声明行显示 `12 端口 · 8 例化 · 被例化 3 次` + `模块信息` 入口；
  `SigRoute: 追踪模块端口…` 可从端口列表直接发起追踪。
- 新的快捷键：`Ctrl+Alt+↑/↓` 追踪上游/下游，`Ctrl+Alt+P` 跳到父层连接，
  `Ctrl+Alt+C` 复制层次路径，树视图内 `Ctrl+Alt+F` 切换过滤。

### 新增 · 解析能力

- **`` `include`` 内联**：把被包含文件的内容原地拼进解析流（含位置映射），
  "端口列表/模块体片段写在 .vh 里"的工程现在能解析完整。可用 `sigroute.inlineIncludes` 关闭。
- **IP 例化模板**：`sigroute.blackboxSources`（默认 `**/*.veo`、`**/*.vho`）里的端口声明
  会被用来补齐黑盒 IP 的方向，且**永不覆盖**工程里的真实实现。
- **generate for 实例名**：还原成 `g_lane[i].u_leaf` 这种真实层次名。
- 修复：端口范围 `[7:0]` 从未被正确切分（早先写作 `!cur.includes('')`，恒为 false），
  导致所有端口的位宽恒为 `null`、范围显示异常。现在位宽/范围正常。

### 新增 · 性能与缓存

- 索引结果落盘缓存（`sigroute.indexCache`）：重启 VSCode 时若文件指纹与配置指纹未变，
  直接接管索引、跳过解析。
- 保存文件时的增量更新改为"只动这个文件贡献的例化记录"，不再遍历全部模块。
- 同文档同名高亮改为按 `(uri, version)` 缓存词法结果，光标移动不再重复全文分析。
- `` `include`` 解析改为 basename 索引查表，去掉 O(includes × files) 的串行探测。

### 新增 · 框图

- 双击方块/连线 → 在树视图（或脑图）里定位到对应节点；被过滤掉时自动切回"全部"。
- 右侧「信号清单」：链路上出现过的网络名，点击即以该信号为起点重新追踪。
- 面板内的方向/跳数选择在换信号时保持，不再每次重置。

### 新增 · 工具

- `node tools/selftest.mjs`：临时目录生成 RTL 夹具，覆盖 include 内联、等价类、层次路径、
  generate 命名、追踪标记、过滤、缓存往返，并用最小 DOM 桩把两个 Webview 真正渲染一遍
  （含折叠、导出、空状态），共 51 项断言。
- `tools/verify.mjs` 的 `--trace` 会打印层次路径，`--describe` 会打印同网络别名。

## [0.1.0] - 2026-09-13

首个版本。围绕"一根信号穿越多个模块时难以跟踪"这个问题，提供从解析到可视化的完整能力。

### 新增 · 解析与索引

- 自研 Verilog/SystemVerilog 结构解析器（零运行时依赖）
  - 模块端口列表（ANSI 与传统两种风格，含方向 / 位宽 / 顺序）
  - 例化与端口连接（命名连接 + 位置连接回填）
  - `assign` / `always` 的语句级数据流（`lhs <= reads`）
  - 正确处理 `(* ... *)` 综合属性（Xilinx 工程必需）
  - 剔除非信号名（循环变量、`genvar`、`parameter`）
- 词法层处理注释 / 字符串 / 宏，并依据 `` `ifdef `` 裁剪条件编译分支
- 工作区索引：模块表、文件→模块、模块→例化点反查；保存时增量更新
- 实测：全工程 551 文件 / 494 模块 / 3019 例化，索引约 1 秒；
  例化匹配 91%、端口名匹配 100%

### 新增 · 信号路由追踪

- 跨模块追踪上游驱动与下游负载，自动识别**改名点**（如 `o_data` ⇄ `s_data`）
- 区分「模块内传播」与「跨模块跳转」，标注位选、表达式、黑盒、多路径、成环等不确定性
- 节点预算与分支上限，保护大扇出信号下的界面流畅度

### 新增 · 树视图

- 资源管理器内的 `SigRoute 信号路由` 面板，点击节点跳转到源码
- 点击后**选中信号名并短暂闪烁高亮**（跳转偏移命中率 100%）

### 新增 · 框图可视化

- 模块级拓扑图：上游在左、下游在右，起点模块高亮
- 三个保持简洁的机制：跳数裁剪（按信号实际传播层数）、并行边合并、节点预算
- 交互：缩放 / 平移 / 点击跳转 / 悬停高亮关联边 / LR-TD 切换 / 导出 SVG
- 实测：某数据总线信号从 31 模块 271 连线收敛到 3 模块 2 连线

### 新增 · 编辑器内能力

- **悬停速查**：位宽、方向、驱动源、负载、**上级模块连接**，每项均可点击跳转
- **悬停端口**可直接跳到父模块例化该端口的位置（多处例化时列出全部）
- `Ctrl+点击` 跳定义、`Shift+F12` 查找引用、同文档同名信号高亮
- 悬停/引用均为只读查询，不会改变面板里正在追踪的主信号

### 新增 · 导出

- Mermaid 流程图、Markdown 路由报告、SVG 框图

### 新增 · 开发与验证工具

- `tools/verify.mjs`：在真实工程上量化解析准确率，支持追踪、框图、跳转偏移校验、Hover 速查预览
- `tools/check.mjs`：无依赖语法自检（Node 原生 type-stripping）
- `tools/make-icon.mjs`：代码生成图标（SDF 抗锯齿）
